name: Multi Event Workflow

on:
    push:
        branches:
            - main
            - dev
    pull_request:
        branches:
            - main

jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: Echo basic informations
              run: |
                echo "REF: $GITHUB_REF"